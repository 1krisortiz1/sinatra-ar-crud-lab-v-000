
<!-- That view should use ERB to iterate over @articles and
render them on the page. -->


<h1>Articles</h1>

<ol>
    <% @articles.each do |article| %> <!-- Loads all articles into an instance variable, render to corresponding invdex view.  -->
        <li><%= article.name %></li>
    <% end %>
</ol>
